{
  "name": "m4sport",
  "private": true,
  "type": "module",
  "scripts": {
    "install-playwright": "PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install --with-deps --only-shell chromium",
    "start": "PLAYWRIGHT_BROWSERS_PATH=0 node src/main.js"
  },
  "dependencies": {
    "playwright": "1.55.1"
  }
}
